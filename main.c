#include "includes/malloc.h"
#include <stdio.h>
#include <string.h>

int	main(void)
{
	char	*str;

	DG("fewwfewef");
//	str = malloc(10);
	return (0);
	strncpy(str, "voici dix", 10);
	printf("%s", str);
	return (0);
}
